version: 2

models:
  - name: dim_supplier
    columns:
      - name: supplier_id
        tests:
          - unique
          - not_null
          - relationships:
              to : ref('dim_person')
              field: person_id

      - name: supplier_name
        tests:
          - not_null
